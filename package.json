{
  "name": "tu-proyecto",
  "version": "1.0.0",
  "main": "dist/server/server/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.backend.json && webpack --config webpack.config.js --mode production",
    "start": "node dist/server/server/server.js"
  },
  "engines": {
    "node": "24.x"
  }
}